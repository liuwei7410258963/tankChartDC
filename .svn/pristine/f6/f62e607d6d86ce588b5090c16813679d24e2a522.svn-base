package com.oket.util.poi;

import java.util.ArrayList;
import java.util.List;

/**
 * 〈实现类〉
 *
 * @author ysh
 * @create 2019/10/15
 * @since 1.0.0
 */
public class ExcelColumnSelectionProviderImpl implements ExcelColumnSelectionProvider {
    @Override
    public List<ExcelColumnSelectionItem> selectionItems(String type) {
        List<ExcelColumnSelectionItem> items = new ArrayList<ExcelColumnSelectionItem>();
        if ("company".equals(type)) {
//            List<JSONObject> jsonObjects= ManageCache.getCompaniesList(1);
//            for (JSONObject jsonObject:jsonObjects){
//                if(jsonObject.getString("rank").split(",").length==3){
//                    items.add(new ExcelColumnSelectionItem(jsonObject.getString("companyId"), jsonObject.getString("companyName")));
//                }
//            }
        }
        return items;
    }
}
